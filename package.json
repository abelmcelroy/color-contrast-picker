{
  "name": "color-contrast-picker",
  "version": "1.0.1",
  "description": "Color picking tool for WCAG 2.1 compliant colors. Finds colors meeting the luminance ratio for any WCAG 2.1 standard, while preserving tone.",
  "main": "lib/index.js",
  "types": "lib/index.d.ts",
  "files": [
    "lib"
  ],
  "scripts": {
    "prepublishOnly": "npm run build",
    "build": "tsc",
    "test": "tap --ts ./test/*.tests.ts"
  },
  "author": "abel-mcelroy",
  "license": "MIT",
  "repository": "abelmcelroy/color-contrast-picker",
  "devDependencies": {
    "@types/tap": "^15.0.7",
    "tap": "^16.3.0",
    "ts-node": "^10.9.1",
    "typescript": "^4.7.4"
  },
  "tap": {
    "no-timeout": true
  }
}
